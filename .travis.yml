dist: trusty
language: node_js

deploy:
  script:
    - npm install
    - npm run build
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: master
